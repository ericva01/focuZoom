name: "Release FucuFlow"

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release Tag (e.g. v0.1.1)'
        required: false
        default: 'v0.1.1'

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          # Apple Silicon macOS (M1/M2/M3/M4) -> produces .dmg and .app
          - platform: "macos-latest"
            target: "aarch64-apple-darwin"
            description: "macOS Apple Silicon (.dmg)"

          # Intel macOS -> cross-compiled on macos-latest (macos-13 has been retired by GitHub)
          - platform: "macos-latest"
            target: "x86_64-apple-darwin"
            description: "macOS Intel (.dmg)"

          # Linux Ubuntu / Debian -> produces .deb and .AppImage
          - platform: "ubuntu-22.04"
            target: "x86_64-unknown-linux-gnu"
            description: "Linux (.AppImage and .deb)"

          # Windows x64 -> produces .exe (NSIS) and .msi
          - platform: "windows-latest"
            target: "x86_64-pc-windows-msvc"
            description: "Windows x64 (.exe and .msi)"

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux system dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Install frontend dependencies
        run: npm ci

      - name: Build with Tauri Action
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.event.inputs.tag || github.ref_name }}
          releaseName: 'FucuFlow ${{ github.event.inputs.tag || github.ref_name }}'
          releaseBody: |
            ## FucuFlow Desktop Multi-Platform Release
            
            Download the native installer for your operating system:
            
            ### 🍎 macOS
            - **Apple Silicon (M1 / M2 / M3 / M4)**: `FucuFlow_..._aarch64.dmg`
            - **Intel Mac**: `FucuFlow_..._x64.dmg`
            
            ### 🐧 Linux
            - **Universal Linux (Recommended)**: `FucuFlow_..._amd64.AppImage` *(Run: `chmod +x FucuFlow*.AppImage && ./FucuFlow*.AppImage`)*
            - **Debian / Ubuntu / Linux Mint**: `fucuflow_..._amd64.deb` *(Run: `sudo dpkg -i fucuflow*.deb`)*
            
            ### 🪟 Windows
            - **Windows 10 / 11 64-bit**: `FucuFlow_..._x64-setup.exe` (or `.msi`)
          releaseDraft: false
          prerelease: false
          args: --target ${{ matrix.target }}
